# ── Glass Theme Config ──
decoration {
    rounding = 30
    shadow {
        enabled = true
        range = 30
        render_power = 4
    }
}
general {
    gaps_in = 15
    gaps_out = 30
    border_size = 1
}
