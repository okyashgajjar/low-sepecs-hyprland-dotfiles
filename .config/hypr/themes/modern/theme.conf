# ── Modern Theme Config ──
decoration {
    rounding = 15
    shadow {
        enabled = true
        range = 20
        render_power = 3
    }
}
general {
    gaps_in = 8
    gaps_out = 20
    border_size = 2
}
