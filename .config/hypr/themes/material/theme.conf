# ── Material Theme Config ──
decoration {
    rounding = 20
    shadow {
        enabled = true
        range = 15
        render_power = 2
    }
}
general {
    gaps_in = 10
    gaps_out = 20
    border_size = 2
}
